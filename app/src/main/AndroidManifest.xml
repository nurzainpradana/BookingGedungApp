<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afifemwe.bookinggedung">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookingGedung">
        <activity android:name=".ui.detailgedung.DetailGedungActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.pemilik.PemilikMainActivity" />
        <activity android:name=".ui.main.customer.CustomerMainActivity" />
        <activity android:name=".ui.signin.SignInActivity" />
        <activity android:name=".ui.signup.SignUpActivity" />
        <activity android:name=".ui.splashscreen.SplashScreenActivity">

        </activity>
        <activity android:name=".ui.main.MainActivity" />
    </application>

</manifest>